# ========================= 基础配置（新开关） =========================
# 是否启用现货（true/false）
SPOT_ENABLED=true
# 是否启用 USDM 永续（true/false）
USDM_ENABLED=true
# 是否启用 COINM 永续（true/false）
COINM_ENABLED=true

# 订阅的最大合约数量（轮询挑选，确保各类型都有代表）
MAX_SYMBOLS=50

# 渲染/补数频率（毫秒）
# 表格刷新间隔（建议 2000ms）
RENDER_INTERVAL_MS=2000
# 5 分钟/30 分钟成交额（计价）回补的轮询间隔（建议 10000ms）
SHORT_VOLUME_INTERVAL_MS=10000
# 24 小时数据与 OI/资金费率回补的轮询间隔（建议 30000ms）
T24_INTERVAL_MS=30000

# 颜色开关：类型列（现货=蓝，USDM=黄，COINM=紫）
TYPE_COLOR=true

# 资金费率“接近 0”阈值（小数表示，例如 0.00005 = 0.005%）。
# 注：涨跌(24h) 已改为严格正负着色（=0 不上色），不使用此阈值。
FUNDING_RATE_EPS=0.00005

# ========================= 报价过滤 =========================
# 现货允许展示的主计价（逗号分隔）。只显示列表内的现货交易对。
ALLOWED_SPOT_QUOTES=USDT,USDC,FDUSD
# 过滤掉的现货计价（高通胀货币等），默认排除 TRY
EXCLUDE_SPOT_QUOTES=TRY
# USDM 合约允许的保证金资产（逗号分隔）
ALLOWED_USDM_QUOTES=USDT,USDC
# COINM 合约允许的计价（逗号分隔）
ALLOWED_COINM_QUOTES=USD

# ========================= Binance 域名覆盖（可选） =========================
# 遇到网络问题或需要加速时，可替换为其他官方镜像域。
# 现货 REST 基础域（会自动补 /api、/sapi 等路径）
BINANCE_SPOT_HOST=api-gcp.binance.com
# USDM REST 基础域（会自动补 /fapi 路径）
BINANCE_FAPI_HOST=fapi.binance.com
# COINM REST 基础域（会自动补 /dapi 路径）
BINANCE_DAPI_HOST=dapi.binance.com
# 现货 WebSocket 域（仅主机:端口，无 wss:// 前缀）
BINANCE_SPOT_WS_HOST=stream.binance.com:9443

# ========================= 代理（可选） =========================
# 若需要通过代理访问，可设置其一（或两者）。示例：https://user:pass@host:port
# 留空则不使用代理。
HTTPS_PROXY=
HTTP_PROXY=

# ========================= API Key（可选） =========================
# 仅在需要访问受限接口时填写。普通行情不需要。
BINANCE_KEY=
BINANCE_SECRET=

# ========================= 说明 =========================
# 1) 本文件仅为示例。请复制为 .env 并按需修改。
# 2) 项目已“抛弃旧开关”，不要再使用诸如 FUTURES_ONLY、PCT_EPS 等旧键。
# 3) CLI：node dist/index.js <Token> [--spot] [--futures] [--max N]
#    Token 输入代币本名即可（如 PEPE/ETH/BTC）。系统会自动识别 1000/1M/1000000 前缀型期货（1INCH 为例外）。
